<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">

        <!-- Existing layout components -->

        <!-- This is the RecyclerView that shows data -->
        <LinearLayout
            android:id="@+id/hide_lin_lay_search_result_rv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:padding="2dp">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/search_result_collection_amount_recyclerview"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginLeft="5dp"
                android:layout_marginTop="35dp"
                android:layout_marginRight="5dp"
                android:padding="0dp"
                android:scrollbars="vertical" />
        </LinearLayout>

        <!-- TextView to show "No data found" message -->
        <TextView
            android:id="@+id/tv_no_data_found"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="No data found for provided name, or phone number, or house number"
            android:textSize="16sp"
            android:textStyle="bold"
            android:visibility="gone" /> <!-- Initially hidden -->

    </LinearLayout>

</LinearLayout>
